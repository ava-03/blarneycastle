/**
 * constants/colors.ts
 * -------------------------------
 * Central color palette for the app.
 * Keeps everything consistent and easy to change later.
 * These come directly from your HomeScreen example.
 * -------------------------------
 */

export const colors = {
  brand: "#0f584d",     // deep green background
  textLight: "#ffffff", // white text on dark backgrounds
  textDark: "#000000",  // dark text (used on light sections)
  pill: "#e8f0ec",      // soft light background for info boxes
};
